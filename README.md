Simple android websocket chat test app powered by ktor client. For successful build add a string 
variable SERVER_URL to local.properties file with url of websocket server, e.g. any public websocket 
echo server